import React from 'react'
// aka shopping session
// we need to access shopping session and then add and post and delete from the cartItemTable and use the shopping session Id as verification that we are deleting from the right table
const Cart = () => {
    return (
        <div>Cart</div>
    )
}
export default Cart
