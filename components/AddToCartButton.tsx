// will have to add the item to the cart
// must be passed in a name or id
// must add to cart, and then we must update the cart to display the item they asked for and quantity and stuf like that
// cant implement stripe until I am deploying this app
// or look up videos but finish today

import React from 'react'

export const AddToCartButton = () => {
    return (
        <div>
            <button>ADD TO CART</button>
        </div>
    )
}